# Bin Packing

Attempt to solve bin packing in python using a brute force method.
Algorithm attempts all possible rotations, then recursively iterates over remaining space,
space is split in a guillotine way.

(only works with equally sized cuboid items)
